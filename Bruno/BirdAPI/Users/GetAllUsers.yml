info:
  name: GetAllUsers
  type: http
  seq: 1

http:
  method: GET
  url: https://birdid-api.onrender.com/users
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
