info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: https://birdid-api.onrender.com/register
  body:
    type: json
    data: |
      {
        "userName": "email",
        "email": "email@email.com",
        "password": "email123",
        "notes": "Ceci est un utilisateur de test.",
        "points": 100,
        "avatar": "0"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
