info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: https://birdid-api.onrender.com/login
  body:
    type: json
    data: |
      {
        "email": "email@email.com",
        "password": "email123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
